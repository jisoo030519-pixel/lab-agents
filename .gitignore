__pycache__/
*.pyc

# 프로그램북 PDF는 용량이 커서 추적하지 않는다 (다시 받으면 된다)
agents/*/programs/
agents/*/scans/

# 생성물
agents/*/reports/
agents/*/calendars/
agents/*/LATEST.md
agents/*/data.json

# OMC 런타임 상태 (운영 아티팩트)
.omc/

# 정적 사이트는 생성물이지만 GitHub Pages 가 서비스하므로 커밋한다 (docs/ 는 제외하지 않음)

# 파일 하나짜리 배포본 — 연차가 들어 있어 저장소에 올리지 않는다
연구실_일정_*.html
